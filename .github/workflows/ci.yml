name: CI

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# Explicitly grant the permissions required by the action
permissions:
  contents: read
  pull-requests: read
  checks: write        # post test annotations

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      list: ${{ steps.resolve.outputs.list }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.github/.nvmrc'
          cache: 'npm'
          cache-dependency-path: .github/package-lock.json
      - name: Install CI script dependencies
        run: npm ci --prefix .github
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4
        id: filter
        with:
          filters: |
            contracts:
              - 'pom.xml'
              - 'contracts/**'
            authorization-server:
              - 'authorization-server/**'
            geo-scoring:
              - 'geo-scoring/**'
            fraud-detection:
              - 'fraud-detection/**'
            decision-engine:
              - 'decision-engine/**'
            gateway:
              - 'gateway/**'

      - name: Resolve modules
        id: resolve
        env:
          FILTER_CONTRACTS: ${{ steps.filter.outputs.contracts }}
          FILTER_AUTHORIZATION_SERVER: ${{ steps.filter.outputs.authorization-server }}
          FILTER_GEO_SCORING: ${{ steps.filter.outputs.geo-scoring }}
          FILTER_FRAUD_DETECTION: ${{ steps.filter.outputs.fraud-detection }}
          FILTER_DECISION_ENGINE: ${{ steps.filter.outputs.decision-engine }}
          FILTER_GATEWAY: ${{ steps.filter.outputs.gateway }}
        run: .github/node_modules/.bin/tsx .github/scripts/resolve-modules.ts
      - name: Debug filter outputs
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Filter contracts: ${{ steps.filter.outputs.contracts }}"
          echo "Filter authorization-server: ${{ steps.filter.outputs.authorization-server }}"
          echo "Filter geo-scoring: ${{ steps.filter.outputs.geo-scoring }}"
          echo "Filter fraud-detection: ${{ steps.filter.outputs.fraud-detection }}"
          echo "Filter decision-engine: ${{ steps.filter.outputs.decision-engine }}"
          echo "Filter gateway: ${{ steps.filter.outputs.gateway }}"
          echo "Resolved list: ${{ steps.resolve.outputs.list }}"

  build-test:
    needs: changes
    if: needs.changes.outputs.list != ''
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build, unit test, and install
        run: |
          ./mvnw -pl "${{ needs.changes.outputs.list }}" -am install -DskipITs --batch-mode --no-transfer-progress

      - name: Integration tests
        run: |
          ./mvnw -pl "${{ needs.changes.outputs.list }}" failsafe:integration-test failsafe:verify --batch-mode --no-transfer-progress

      # ── Test Reports ─────────────────────────────────────────────
      - name: Publish unit test report
        uses: dorny/test-reporter@a43b3a5f7366b97d083190328d2c652e1a8b6aa2 # v3
        if: success() || failure()    # run even if tests fail
        with:
          name: Unit Tests
          path: '*/target/surefire-reports/TEST-*.xml'
          reporter: java-junit
          fail-on-empty: false    # a changed module may have only ITs (no surefire unit tests)

      - name: Publish integration test report
        uses: dorny/test-reporter@a43b3a5f7366b97d083190328d2c652e1a8b6aa2 # v3
        if: success() || failure()
        with:
          name: Integration Tests
          path: '*/target/failsafe-reports/TEST-*.xml'
          reporter: java-junit
          fail-on-empty: false